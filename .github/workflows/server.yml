name: Server

on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: self-hosted

  strategy:
    matrix:
      node-version: [16.x]

  steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
    - run: npm install
    - run: pm2 restart App
